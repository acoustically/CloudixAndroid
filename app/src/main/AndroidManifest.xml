<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="acoustically.cloudix">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".MainActivity"/>
    <activity android:name=".Sign.SignUpGetIdActivity"/>
    <activity android:name=".Sign.SignUpGetPasswordActivity"/>
    <activity android:name=".Sign.SignInGetIdActivity"/>
    <activity android:name=".Sign.SignInGetPasswordActivity"/>
    <activity android:name=".IndexActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".AddIOTDevice.AddIOTDeviceActivity">
    </activity>
    <activity android:name=".AddIOTDevice.AddIOTDeviceActivity1">
    </activity>
    <activity android:name=".AddIOTDevice.AddIOTDeviceActivity2">
    </activity>
  </application>

</manifest>